{
  "name": "searxng",
  "display_name": "SearXNG MCP Server",
  "description": "A Model Context Protocol Server for [SearXNG](https://docs.searxng.org/)",
  "repository": {
    "type": "git",
    "url": "https://github.com/ihor-sokoliuk/mcp-searxng"
  },
  "homepage": "https://github.com/ihor-sokoliuk/mcp-searxng",
  "author": {
    "name": "ihor-sokoliuk"
  },
  "license": "MIT",
  "categories": [
    "web"
  ],
  "tags": [
    "search",
    "searxng",
    "api"
  ],
  "installations": {
    "npm": {
      "type": "npm",
      "command": "npx",
      "args": [
        "-y",
        "${REPO_PATH}"
      ],
      "env": {
        "SEARXNG_URL": "${SEARXNG_URL}"
      }
    }
  },
  "arguments": {
    "REPO_PATH": {
      "description": "Full path to the MCP SearXNG repository that is being installed.",
      "required": true,
      "example": "/path/to/mcp-searxng"
    },
    "SEARXNG_URL": {
      "description": "Environment variable to set the URL of the SearXNG instance that will be used for search queries.",
      "required": true,
      "example": "http://localhost:8080"
    }
  }
}